apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: weaviates.weaviate.io
spec:
  group: weaviate.io
  scope: Namespaced
  names:
    plural: weaviates
    singular: weaviate
    kind: Weaviate
    shortNames:
      - wv
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                exampleField:
                  type: string

# NOTE: This is a minimal placeholder CRD. Replace with the operator's real CRD
# from the upstream repository before deploying to production.
