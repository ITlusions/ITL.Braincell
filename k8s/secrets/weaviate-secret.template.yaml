apiVersion: v1
kind: Secret
metadata:
  name: braincell-weaviate-secret
  namespace: braincell
type: Opaque
stringData:
  # Weaviate API key (if authentication enabled)
  api-key: "CHANGE_ME_WEAVIATE_API_KEY"
  # OIDC configuration
  oidc-client-secret: "CHANGE_ME_OIDC_CLIENT_SECRET"
